apiVersion: v1
kind: Service
metadata:
  name: frontend-nodeport-service
  labels:
    app: hello
spec:
  type: NodePort
  selector:
    app: hello
    tier: frontend
    track: stable
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30008
